{
  "_comment": [
    "This is an EXAMPLE — copy to .mcp.json (project-level) or merge the",
    "'beams' entry into ~/.claude.json (user-level).  For user-level config",
    "replace './channel/beams-channel.mjs' with the absolute path to that",
    "file, e.g. '/home/you/projects/beams/channel/beams-channel.mjs'.",
    "",
    "After registering, launch with the development flag (required for any",
    "community plugin not yet on Anthropic's channel allowlist):",
    "  claude --dangerously-load-development-channels server:beams",
    "",
    "See channel/README.md for the full setup and watcher wiring steps."
  ],
  "mcpServers": {
    "beams": {
      "command": "node",
      "args": ["./channel/beams-channel.mjs"]
    }
  }
}
